# Logstash pipeline — Meridian Sentinel transaction ingestion
# Day 7 will implement the full ECS normalisation and PII obfuscation filters.
# This placeholder ensures docker-compose starts without a missing file error.

input {
  tcp {
    port => 5000
    codec => json_lines
  }
}

filter {
  # Full ECS mapping and PII obfuscation to be implemented on Day 7
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    user => "elastic"
    password => "${ELASTIC_PASSWORD}"
    index => "meridian-transactions-%{+YYYY.MM.dd}"
  }
  stdout { codec => rubydebug }
}
