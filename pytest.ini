[pytest]
# Register custom markers so pytest does not warn about unknown marks.
# Run integration tests with:  pytest tests/ -v -m integration
# Skip integration tests:      pytest tests/ -v -m "not integration"
markers =
    integration: marks tests that require live Docker services (elasticsearch, lstm-serving)
